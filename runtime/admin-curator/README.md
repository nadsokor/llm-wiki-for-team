# Admin Curator Runtime

This directory stores local runtime data for the administrator host.

Contents:

- `state/curator-state.json`: source counters, processed fingerprints, and note counters
- `logs/`: optional runtime logs

This directory is intentionally outside the shared vault.
